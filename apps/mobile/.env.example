EXPO_PUBLIC_APP_SCHEME=signtalk-ai
# Separate endpoints using commas, spaces, or new lines. The app probes every
# endpoint at startup and uses the first reachable VSL-30 V4.3 service.
EXPO_PUBLIC_EDGE_AI_URLS=http://UNO_Q_IP:8082,http://localhost:8082
